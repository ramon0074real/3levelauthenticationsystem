Goto command prompt and install the following line by line.
pip install opencv-python
pip install numpy
pip install scikit-image
pip install skeletonize
pip install matplotlib
pip install sklearn
pip install cmake
pip install pygame
